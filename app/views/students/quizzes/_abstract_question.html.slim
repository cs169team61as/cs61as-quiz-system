em This question is abstract and NOT supposed to be instantiated
- q = sf.object.question
= sf.text_area :content, label: false, class: 'textedit expand saves', id: "#{q.id}", placeholder: "It's a bug, not a feature"